# 09 — README и финальная сверка

**What to build:** пакет документирован и подтверждён как единое целое — независимость, риски guard'а, использование во всех трёх формах группировки.

**Blocked by:** 05, 06, 07, 08

**Status:** ready-for-agent

- [ ] README: установка, регистрация через Laravel autodiscovery, пример объявления модели для всех трёх сценариев §7 (составной ключ, одна колонка, пустой список), пример `placeInto()` и всех обёрток, пример `scopeOrdered()`
- [ ] README, раздел «Что guard не защищает»: не видит `insert()`, `upsert()`, `withoutEvents()` и любые bulk-запросы в обход `saving`; настоящая гарантия целостности — уникальный индекс на уровне БД, guard — только удобство разработчика
- [ ] README: инструкция по `list-ordering:check-index` и `assertOrderingIndexMatches()`
- [ ] Финальный прогон: `composer test` зелёный без единого проекта-потребителя в окружении (testbench-изоляция) — по всем драйверам матрицы из тикета 08
- [ ] Финальная сверка: оба CI-гейта независимости из тикета 01 всё ещё зелёные на полном `src/`+`tests/` пакета (а не только на пустом дереве, как проверялось в 01)
