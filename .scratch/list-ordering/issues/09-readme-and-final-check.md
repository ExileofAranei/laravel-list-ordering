# 09 — README и финальная сверка

**What to build:** пакет документирован и подтверждён как единое целое — независимость, риски guard'а, использование во всех трёх формах группировки.

**Blocked by:** 05, 06, 07, 08

**Status:** ready-for-agent

- [x] README: установка, регистрация через Laravel autodiscovery, пример объявления модели для всех трёх сценариев §7 (составной ключ, одна колонка, пустой список), пример `placeInto()` и всех обёрток, пример `scopeOrdered()`
- [x] README, раздел «Что guard не защищает»: не видит `insert()`, `upsert()`, `withoutEvents()` и любые bulk-запросы в обход `saving`; настоящая гарантия целостности — уникальный индекс на уровне БД, guard — только удобство разработчика
- [x] README: инструкция по `list-ordering:check-index` и `assertOrderingIndexMatches()`
- [x] Финальный прогон: `composer test` зелёный без единого проекта-потребителя в окружении (testbench-изоляция) — 74 пройдено + 2 ожидаемо skip (collation-critical на SQLite, по дизайну) на SQLite; 76/76 пройдено на реальном PostgreSQL (поднят локально, см. тикет 08); MySQL/MariaDB подтверждаются только в GitHub Actions (нет сети на образы в этой песочнице — задокументировано, не выдано за проверенное)
- [x] Финальная сверка: оба CI-гейта независимости из тикета 01 всё ещё зелёные на полном `src/`+`tests/` пакета (37 файлов, а не пустое дерево, как в тикете 01)

**Попутно:** README пакета был почти целиком нетронутым skeleton-заглушкой Spatie — битые ссылки на бейджи (`spatie/package-laravel-list-ordering-laravel`), пример `echoPhrase()` на несуществующем классе, разделы публикации миграции/конфига/views для артефактов, удалённых ещё в тикете 01, и промо-блок «Support us» с рекламой Spatie. Переписан полностью; ссылка на отсутствующий `CONTRIBUTING.md` убрана, а не оставлена битой.
