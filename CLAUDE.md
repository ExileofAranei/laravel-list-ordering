## Language

Respond to the user in Russian, always — including in headings, summaries, and status updates. Code, identifiers, and comments stay in English as usual; only prose communication with the user is Russian.

## Agent skills

### Issue tracker

Issues live in GitHub Issues (ExileofAranei/laravel-list-ordering), via the `gh` CLI. See `docs/agents/issue-tracker.md`.

### Domain docs

Single-context layout — `CONTEXT.md` + `docs/adr/` at the repo root. See `docs/agents/domain.md`.
